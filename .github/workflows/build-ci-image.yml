name: Build CI Base Image

# Rebuild the pre-baked dependency image whenever the Dockerfile or
# test requirements change, and allow manual triggering.
on:
  push:
    branches:
      - master
    paths:
      - Dockerfile.ci
      - requirements-test.txt
      - requirements.txt
  workflow_dispatch:

concurrency:
  group: build-ci-image-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-push:
    name: Build and push CI base image to GHCR
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # BuildKit cache stored in GitHub Actions cache — subsequent builds
      # only rebuild layers that actually changed (e.g. only the npm layer
      # when prettier is bumped, not the whole apt layer).
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.ci
          push: true
          # BuildKit layer cache in GHA cache storage
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/${{ github.repository }}/ci-base:latest
            ghcr.io/${{ github.repository }}/ci-base:sha-${{ github.sha }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ github.event.head_commit.timestamp }}

      - name: Verify image
        run: |
          docker pull ghcr.io/${{ github.repository }}/ci-base:latest
          docker run --rm ghcr.io/${{ github.repository }}/ci-base:latest \
            bash -c "gdal-config --version && node --version && prettier --version"
